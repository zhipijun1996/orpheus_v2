# PROJECT_STATE_BOOTSTRAP v0.1 — Report

## Verdict

**Formal bootstrap tests: PASS**

This package is the first complete Git-ready Project State layout, not a test fixture.

## What became Active State

- Generic Harness v0.6.2 (project-specific Calibration moved out of active Harness)
- PROJECT_BRIEF
- atomic Shared working state
- Cheng / Lin / Zhou / Daughter character states
- FLESH / ECHO / ORIGIN / BLINDSPOT / NOBODY / NULL route states
- ordinary Route Interfaces
- CH2 shared scene candidate
- Open Questions
- Integration Debt
- Human Decisions
- Artifact Registry

## What did NOT become Canon

- FLESH's exact ending
- ECHO daughter overlay
- ORIGIN's unresolved motive
- BLINDSPOT repair-loop / NULL-door ideas
- NOBODY's `IDENTITY: NULL` mechanism or `Zhou = NULL` Working Truth
- Daughter DNA explanation
- NULL Meta answer

These remain Candidate/Open and are not default creative context unless a task selects them.

## Important migration correction

The original v0.6.2 Harness package contained project-specific Calibration examples.
In this formal repo they have been relocated to `archive/calibration/` so active `harness/` is project-agnostic.

## Legacy handling

Old G2/Codex-handoff documents are preserved under `archive/legacy_baselines/`.
They no longer have authority because they encode ECHO-like mechanics as global objective truth.

## Current development risk map

1. ORIGIN — critical Story Engine gap.
2. NOBODY — must close a complete local Anomaly Ledger.
3. BLINDSPOT — mechanism closes, but final successful-change consequence / value fork is open.
4. Shared Ch2 — must stay observational enough for FLESH/ECHO/BLINDSPOT/NOBODY simultaneously.
5. NULL Meta — deliberately deferred.

## Context routing

The generated Context Pack uses:
`Harness Core + Project Brief + Human Decisions + target Route + target Interface + direct character/shared dependencies`.

Archive/Sprints/Calibration/legacy baselines are rejected from ordinary packs.

## Test results

### Harness lint: PASS
```
HARNESS LINT: PASS
Core rules: 9
Core chars: 1031
Active policy chars: 17169
```

### Project lint: PASS
```
PROJECT LINT: PASS
```

### Registry validation: PASS
```
REGISTRY: PASS
```

## Recommended next migration step

Do not add more design before the repository contract is exercised once.

Next controlled Codex pilot should be a bounded branch task that:
1. reads a generated Context Pack,
2. develops one target only,
3. produces a Sprint artifact,
4. updates Route State / Interface / Open Questions / Registry,
5. archives the Sprint after distillation,
6. stops at Human Gate without self-promoting a decision.

ORIGIN is the best creative target for that pilot because it is genuinely underdeveloped; NOBODY is a good Closure/ledger target afterward.
