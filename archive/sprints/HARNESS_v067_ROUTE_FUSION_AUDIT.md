# Harness v0.6.7 — Route Fusion & Mystery Closure Audit

## OBSERVE
A Route Engine can pass spine, readability, mechanism, and local-coherence checks while still using the shared mystery as scenery. The motivating case touched the shared accident through evidence recovery but left the central NULL mystery, a core character, and the Route-defining speculative meaning weak or detachable.

## LOCALIZE
- `PROJECT_DATA`: define required core mysteries and ORIGIN-specific functional dimensions without seeding solution options.
- `TEMPLATE/INTERFACE`: separate Shared openness from Route engagement/closure.
- `MODULE`: refine Story/Reveal review around causal fusion and Evidence != Truth.
- `CORE`: unchanged.

## PATCH
- add `Story Engine / Route Truth / Evidence` separation;
- require local closure of core Route mysteries while Shared answers remain open across Routes;
- define full NULL answer contract beyond identity;
- add Route Engine fusion diagnostics with STRONG/PARTIAL/DETACHED, failing only DETACHED;
- require core-cast causal function without forcing every long-term outcome closed;
- replace option-like `must_remain_open` lists with dimension-only `shared_open_dimensions` plus explicit `route_dimensions`;
- remove the fixed adult-at-2054 daughter prior from active Project State.

## ANTI-OVERFIT
No active creative context says who NULL is, whether any daughter is NULL, whether physical return occurs, or which exact Argo mechanism is correct. Calibration history remains quarantined. PARTIAL fusion is a risk, not automatic failure.

## REGRESS
- motivating evidence-heavy detached family story => FAIL/ROLLBACK_PARENT;
- high-fusion temporal relationship story => survives without prescribing its identity solution;
- non-physical or zero-future-intervention Routes remain eligible if they close required mysteries through their own local truth;
- strong new mechanisms remain protected by v0.6.6 leverage discipline.

## STATUS
Promote to STABLE after Harness lint, Project lint, Registry validation, semantic Context-Pack checks, and active-policy budget pass.
